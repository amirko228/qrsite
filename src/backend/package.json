{
  "name": "socialqr-backend",
  "version": "1.0.0",
  "description": "Backend API for SocialQR",
  "main": "main.py",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 